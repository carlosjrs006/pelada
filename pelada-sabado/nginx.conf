server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # Redireciona todas as rotas para o Angular (SPA)
    location / {
        try_files $uri $uri/ /index.html;
    }
}
